() => {
  console.log('Highlighting');

  const highlight = () => {
    // Select elements previously selected
    // Clear all styling
    // Find new elements in query.
    // Re-apply styles
  };

  highlight();
};
